name: .NET CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - run: dotnet restore FileSystemManager.sln
      - run: dotnet build FileSystemManager.sln --no-restore --configuration Release
      - run: dotnet test FileSystemManager.sln --no-build --configuration Release